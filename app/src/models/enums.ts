export enum UserRole {
  ADMIN = 'admin',
  EMPLOYEE = 'employee',
}

export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
}

export enum GenderLanguage {
  male = 'Erkek',
  female = 'Kadın',
}

export enum DrawerType {
  Add,
  Update,
}

export enum ExhangeRateType {
  UP = 'up',
  DOWN = 'down',
}

export enum ExhangeRateValueType {
  BUYING = 'buying',
  SALES = 'sales',
}

export enum OrderType {
  ASC = 'asc',
  DESC = 'desc',
}
